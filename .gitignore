# Docker service logs
logs/*.log

# Python caches (and any stray SQLite file — the server used to keep one here)
*.db
__pycache__/
*.pyc

# Local venv for editor tooling only (the app runs in Docker)
.venv/
