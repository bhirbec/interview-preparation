{
  "difficulty": "medium",
  "tags": [
    "recursion",
    "bit-manipulation",
    "math"
  ],
  "hint": "use the identity a * b = (a * (b >> 1)) << 1, adding one extra `a` when the low bit of b is set. This halves b at every step, giving O(log b) recursive calls. Examples: multiply(0, 5) -> 0 multiply(7, 1) -> 7 multiply(3, 4) -> 12 multiply(9, 9) -> 81"
}
