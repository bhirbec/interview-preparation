{
  "difficulty": "medium",
  "tags": [
    "linked-list",
    "two-pointers",
    "runner"
  ],
  "hint": "walk each list to find its tail and length. If the tails are not the same object the lists cannot intersect. Otherwise advance a pointer in the longer list by the length difference, then advance both pointers in lockstep until they reference the same node -- that node is the intersection."
}
