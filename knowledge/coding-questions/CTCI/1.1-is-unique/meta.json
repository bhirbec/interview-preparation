{
  "difficulty": "easy",
  "tags": [
    "string",
    "hashtable",
    "bit-manipulation"
  ],
  "hint": "track which characters have already been seen. unique_char uses a boolean lookup table indexed by character code; unique_char_bitvector packs the same \"seen\" flags into a single integer used as a bit vector. If a string is longer than the number of possible distinct characters, it cannot be unique."
}
