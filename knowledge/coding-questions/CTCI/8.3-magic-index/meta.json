{
  "difficulty": "medium",
  "tags": [
    "array",
    "binary-search",
    "recursion"
  ],
  "hint": "because the values are distinct and sorted, a binary search works. If array[mid] > mid the magic index (if any) must be on the left; if array[mid] < mid it must be on the right."
}
