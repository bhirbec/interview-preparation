# Longest Substring Without Repeating Characters
#
# You are given a string `s`. Return the length of the longest substring
# (contiguous run of characters) of `s` that contains no repeated character.
#
# Constraints:
#   - 0 <= len(s) <= 5 * 10^4
#   - s consists of English letters, digits, symbols and spaces
#
# Examples:
#   "abcabcbb" -> 3   ("abc")
#   "bbbbb"    -> 1   ("b")
#   "pwwkew"   -> 3   ("wke"; "pwke" is a subsequence, not a substring)
#   ""         -> 0


def longest_unique_substring_length(s):
  # TODO: implement
  raise NotImplementedError
